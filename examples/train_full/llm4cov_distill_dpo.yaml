### model
model_name_or_path: Qwen/Qwen3-4B-Instruct-2507
# model_name_or_path: /path/to/your/sft_or_base_checkpoint
trust_remote_code: true

### method
stage: dpo
do_train: true
finetuning_type: full
deepspeed: examples/deepspeed/ds_z3_offload_config.json
pref_beta: 0.1
pref_loss: sigmoid
pref_ftx: 0.0

### dataset
dataset: llm4cov_distill_dpo
template: qwen3_nothink
cutoff_len: 40960
# max_samples: 8  # debug
overwrite_cache: false
preprocessing_num_workers: 16
dataloader_num_workers: 4

### output
output_dir: /data/hejia/dpo_backup/qwen3_4b_llm4cov_distill_dpo
logging_steps: 1
save_steps: 200
plot_loss: true
overwrite_output_dir: false
save_only_model: false

### train
per_device_train_batch_size: 1
gradient_accumulation_steps: 4
learning_rate: 1.0e-5
num_train_epochs: 1.0
lr_scheduler_type: cosine
warmup_ratio: 0.03
bf16: true
ddp_timeout: 180000000
seed: 42
optim: adamw_torch
flash_attn: fa2
resume_from_checkpoint: null

report_to: wandb
run_name: qwen3_4b_llm4cov_distill_dpo

### eval (optional)
# eval_dataset: llm4cov_distill_dpo
# val_size: 0.02
# per_device_eval_batch_size: 1
# eval_strategy: steps
# eval_steps: 2000
